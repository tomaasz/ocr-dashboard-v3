# --- Project identity ---
sonar.projectKey=tomaasz_ocr-dashboard-v3
sonar.projectName=OCR Dashboard V3
sonar.projectVersion=1.0

# --- SonarQube (self-hosted) ---
sonar.host.url=http://54.37.252.57:9000
# sonar.login is handled via SONAR_TOKEN env var

# --- Sources & tests ---
sonar.sources=app,src,run.py
sonar.tests=tests

# --- Python ---
sonar.python.version=3.12
sonar.python.coverage.reportPaths=coverage.xml
sonar.sourceEncoding=UTF-8

# --- Exclusions ---
sonar.exclusions=**/venv/**,**/.venv/**,**/__pycache__/**,**/.git/**,logs/**,artifacts/**,.agent/**,**/.tox/**,**/.mypy_cache/**,**/.pytest_cache/**,**/migrations/**
